You are executing MA-11 (Validate GCC Core Dev Image).

Goals:
1) Validate GCC core dev image using canonical validation script.
2) Capture telemetry/log evidence during validation execution window.
3) Report binary PASS/FAIL with blocker mapping.

Required path:
- `./.agents/skills/cpp26-dev-image-validate/scripts/test_images.sh --toolchain gcc --flavor core --image-tag dev`

Deliverables:
- GCC core image validation outcome
- Telemetry/log evidence
- Binary PASS/FAIL with blocker mapping

Completion requirements:
- Write result file: `.codex/multi-agent/results/MA-11-result.md`
- Include required sections:
  - `## Findings`
  - `## Evidence Commands`
  - `## PASS/FAIL`
  - `## Blockers (Owner Thread)`
  - `## Learnings`
- Run thread validation commands before final PASS/FAIL.
- Run: `./.codex/multi-agent/scripts/sync_agents_learnings.sh`
- If PASS, commit this thread's changes and include commit evidence commands in the result:
  - `git status --short`
  - `git add ...`
  - `git commit -m "ma: complete MA-11 validate gcc core dev image"`
  - `git show --name-only --oneline -n 1`
