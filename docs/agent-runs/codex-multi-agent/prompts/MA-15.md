You are executing MA-15 (Benchmark Surface).

Load these skills first:
- $cpp-devcontainer-bake
- $pixi-devcontainer
- $python-pixi-astral-toolchain
- $docker-expert
- $verification-before-completion

Primary source of truth:
- `spec/2026-03-23-devcontainer-build-optimization-spec.md`
- `Dockerfile`
- `docker-bake.hcl`
- `pixi.toml`
- `README.md`

Required write scope:
- `scripts/benchmark-devcontainer-build.sh`
- `scripts/report-devcontainer-size.sh`
- `benchmarks/devcontainer/schema.json`
- `benchmarks/devcontainer/README.md`
- `pixi.toml`

Goals:
1) Implement the V1 benchmark schema and benchmark scripts exactly within the spec's measured surface.
2) Add the size-report script and leaf `pixi` tasks.
3) Ensure artifacts record enough metadata for valid paired-run comparisons.
4) Keep the surface small, shell-first, and reproducible.

Deliverables:
- Benchmark schema
- Benchmark runner script
- Image size report script
- `pixi` task surface for smoke/benchmark entry points

Constraints:
- Do not implement deferred metrics from the spec.
- Do not add a new Python package module or repo-wide orchestration layer.
- Keep edits inside the required write scope.
- Favor deterministic metadata and explicit output fields over convenience abstractions.

Completion requirements:
- Write result file: `.codex/multi-agent/results/MA-15-result.md`
- Include required sections:
  - `## Findings`
  - `## Evidence Commands`
  - `## PASS/FAIL`
  - `## Blockers (Owner Thread)`
  - `## Learnings`
- Include telemetry/log evidence commands and observed-output summary in the result.
- Run thread validation commands before final PASS/FAIL.
- Run: `./.codex/multi-agent/scripts/sync_agents_learnings.sh`
- If PASS, commit this thread's changes and include commit evidence commands in the result:
  - `git status --short`
  - `git add scripts/benchmark-devcontainer-build.sh scripts/report-devcontainer-size.sh benchmarks/devcontainer/schema.json benchmarks/devcontainer/README.md pixi.toml`
  - `git commit -m "ma: complete MA-15 benchmark surface"`
  - `git show --name-only --oneline -n 1`
