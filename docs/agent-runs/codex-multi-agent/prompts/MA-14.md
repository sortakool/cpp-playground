You are executing MA-14 (Authoritative Smoke Gate).

Load these skills first:
- $cpp-devcontainer-bake
- $docker-expert
- $pixi-devcontainer
- $verification-before-completion

Primary source of truth:
- `spec/2026-03-23-devcontainer-build-optimization-spec.md`
- `Dockerfile`
- `docker-bake.hcl`
- `.devcontainer/devcontainer.json`
- `.github/workflows/tooling-validate.yml`
- `.github/workflows/latest-kernel-ci.yml`
- `verification/verification.toml`

Required write scope:
- `.github/workflows/devcontainer-authoritative-smoke.yml`
- `scripts/smoke-devcontainer-image.sh`

Goals:
1) Implement the required authoritative Linux `amd64` devcontainer smoke workflow.
2) Implement a repo-owned smoke script for the built `devcontainer` image.
3) Keep the workflow aligned with the existing self-hosted `latest-kernel` runner surface.
4) Do not weaken proof if authoritative runtime execution is unavailable.

Deliverables:
- New workflow file
- Repo-owned smoke script
- Exact validation commands and PASS/FAIL

Constraints:
- Do not edit files outside the required write scope.
- The workflow must target `self-hosted`, `Linux`, `X64`, `latest-kernel`.
- Use repo-owned commands/scripts rather than embedding large one-off shell logic in the workflow.
- Do not add a hosted fallback for authoritative proof in this phase.
- Preserve the current Dockerfile/stage contract.

Completion requirements:
- Write result file: `.codex/multi-agent/results/MA-14-result.md`
- Include required sections:
  - `## Findings`
  - `## Evidence Commands`
  - `## PASS/FAIL`
  - `## Blockers (Owner Thread)`
  - `## Learnings`
- Include telemetry/log evidence commands and observed-output summary in the result.
- Run thread validation commands before final PASS/FAIL.
- Run: `./.codex/multi-agent/scripts/sync_agents_learnings.sh`
- If PASS, commit this thread's changes and include commit evidence commands in the result:
  - `git status --short`
  - `git add .github/workflows/devcontainer-authoritative-smoke.yml scripts/smoke-devcontainer-image.sh`
  - `git commit -m "ma: complete MA-14 authoritative smoke gate"`
  - `git show --name-only --oneline -n 1`
