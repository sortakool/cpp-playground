You are executing MA-07 (Validate Core Dev Images).

Goals:
1) Validate core dev images using canonical validation script.
2) Enforce scope locks: clang required, gcc optional, quantlib out-of-scope, publish out-of-scope.
3) Capture telemetry/log evidence during validation execution window.

Required validation path:
- `./.agents/skills/cpp26-dev-image-validate/scripts/test_images.sh --toolchain clang --flavor core --image-tag dev`

Optional validation path (only if gcc image is available):
- `./.agents/skills/cpp26-dev-image-validate/scripts/test_images.sh --toolchain gcc --flavor core --image-tag dev`

Deliverables:
- Validation outcomes for required + optional paths
- Scope compliance evidence
- Binary PASS/FAIL with blocker mapping

Completion requirements:
- Write result file: `.codex/multi-agent/results/MA-07-result.md`
- Include required sections:
  - `## Findings`
  - `## Evidence Commands`
  - `## PASS/FAIL`
  - `## Blockers (Owner Thread)`
  - `## Learnings`
- Run thread validation commands before final PASS/FAIL.
- Run: `./.codex/multi-agent/scripts/sync_agents_learnings.sh`
- If PASS, commit this thread's changes and include commit evidence commands in the result:
  - `git status --short`
  - `git add ...`
  - `git commit -m "ma: complete MA-07 validate core dev images"`
  - `git show --name-only --oneline -n 1`
