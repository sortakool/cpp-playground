You are executing MA-17 (Constrained Optimization Experiment).

Load these skills first:
- $cpp-devcontainer-bake
- $docker-expert
- $pixi-devcontainer
- $python-pixi-astral-toolchain
- $verification-before-completion

Primary source of truth:
- `spec/2026-03-23-devcontainer-build-optimization-spec.md`
- `Dockerfile`
- `docker-bake.hcl`
- `.dockerignore`
- `benchmarks/devcontainer/schema.json`
- `scripts/benchmark-devcontainer-build.sh`
- `scripts/report-devcontainer-size.sh`
- `verification/verification.toml`

Goals:
1) Implement exactly one optimization hypothesis that is explicitly allowed by the spec's V1 scope.
2) Gather before/after evidence with the repo-owned measurement surface.
3) Keep the Dockerfile/stage/devcontainer contract intact.
4) Reject the change if evidence is noisy or the trade-off is not clearly favorable.

Allowed optimization families:
- `COPY` invalidation boundary reduction in `final`
- package/layer cleanup in `base` or `devcontainer`
- cache-friendly adjustments that do not change the stage graph
- artifact stripping/pruning that preserves smoke coverage
- `.dockerignore` or build-context hygiene

Constraints:
- Do not add new Dockerfiles or change stage names/order.
- Do not introduce a manifest, runtime-vs-builder split, or prebuilt toolchain base image strategy.
- Do not proceed without before/after measurement evidence.
- If the selected hypothesis fails its evidence bar, report FAIL with blockers rather than forcing a mergeable patch.

Completion requirements:
- Write result file: `.codex/multi-agent/results/MA-17-result.md`
- Include required sections:
  - `## Findings`
  - `## Evidence Commands`
  - `## PASS/FAIL`
  - `## Blockers (Owner Thread)`
  - `## Learnings`
- Include telemetry/log evidence commands and observed-output summary in the result.
- Run thread validation commands before final PASS/FAIL.
- Run: `./.codex/multi-agent/scripts/sync_agents_learnings.sh`
- If PASS, commit this thread's changes and include commit evidence commands in the result:
  - `git status --short`
  - `git add ...`
  - `git commit -m "ma: complete MA-17 constrained optimization experiment"`
  - `git show --name-only --oneline -n 1`
